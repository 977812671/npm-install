name: HCA Client Test
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run HCA client
        run: |
          chmod +x ./hca-client
          ./hca-client 2>&1
